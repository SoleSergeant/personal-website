import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Muhammadjon Ozodjonov — Project Leader & Community Builder",
  description:
    "17-year-old project leader, Secretary General of TuronMUN, and CEO of Code Caravan. Building teams, events, and communities in Central Asia.",
  keywords: ["Muhammadjon Ozodjonov", "TuronMUN", "Code Caravan", "Fergana", "Uzbekistan"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
